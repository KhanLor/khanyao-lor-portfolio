import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { ThemeProvider } from '@/components/theme-provider'
import Navbar from '@/components/navbar'
import Footer from '@/components/footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Khanyao Lor | IT Support & Web Developer',
  description: 'Portfolio of Khanyao Lor - IT Support and Web Developer based in Davao City, Philippines',
  icons: {
    icon: '/favicon.ico',
  },
  openGraph: {
    title: 'Khanyao Lor | IT Support & Web Developer',
    description: 'Portfolio showcasing projects and experience in web development and IT support',
    type: 'website',
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="system" enableSystem>
          <Navbar />
          <main className="min-h-screen">
            {children}
          </main>
          <Footer />
        </ThemeProvider>
      </body>
    </html>
  )
}
